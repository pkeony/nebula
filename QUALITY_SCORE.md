# Nebula — 품질 기준

> 코드 품질 체크리스트. 피처 완료 시 참고.

## 체크리스트
- [ ] TypeScript strict, `any` 0개
- [ ] 타입체크 통과 (`tsc --noEmit`)
- [ ] 테스트 존재 & 통과
- [ ] 에러 핸들링 — 외부 API 호출에 적절한 에러 메시지
- [ ] 환경변수 — `.env.example` 에 키 목록 유지
