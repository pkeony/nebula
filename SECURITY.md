# Nebula — 보안 규칙

## API 키 관리
- API 키는 `.env` 파일에만 저장 (git 추적 금지)
- 코드에 하드코딩 금지
- `.env.example` 에 키 이름만 기록 (값은 비움)

## 금지 사항
- 시크릿을 로그에 출력 금지
- 클라이언트 사이드에 API 키 노출 금지
- `eval()`, `Function()` 생성자 사용 금지
